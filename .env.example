# GroQ API Configuration
GROQ_API_KEY=your_groq_api_key_here
GROQ_MODEL=openai/gpt-oss-20b
GROQ_MAX_TOKENS=2048

# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your_supabase_publishable_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Email Configuration (for notifications)
RESEND_API_KEY=your_resend_api_key
EMAIL_FROM=Opportune <onboarding@resend.dev>

# Cron Secret (for scheduled jobs)
CRON_SECRET=your_random_secret_for_cron_jobs

# Scraper Configuration
RUN_AGENTIC_CRAWLER=false  # Set to true to enable the AI-directed crawler
USE_MULTI_AGENT_PIPELINE=false  # Set to true to use the new multi-agent pipeline instead of legacy
