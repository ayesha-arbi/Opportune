"""Opportune scraper package.

Run as a module from the repo root: python -m scraper.main
(never `python scraper/main.py` — that breaks the absolute
`from scraper... import` style used throughout).
"""
